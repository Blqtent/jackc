
class UnitString2 {
	function boolean isOk(int l) {
		var String s;
		var int i;
		let l = l + 1;
		let s = String2.copy("Hello World");
		let s = String2.appendStr(s, "!!!");
		do s.dispose();
		//let s = String2.appendStr("GOTCHA", "!!!");
		//do s.dispose();
		do Unit.log(l + 1, "String2.copy...", true);
		if (String2.compare("AA", "BA") < 0) {
			do Unit.log(l, "String2.compare < ok...", true);
		} else {
			do Unit.log(l, "String2.compare < fail...", false);
		}
		if (String2.compare("BA", "AA") > 0) {
			do Unit.log(l, "String2.compare > ok...", true);
		} else {
			do Unit.log(l, "String2.compare > fail...", false);
		}
		if (String2.compare("AA", "AA") = 0) {
			do Unit.log(l, "String2.compare = ok...", true);
		} else {
			do Unit.log(l, "String2.compare = fail...", false);
		}
		if (String2.compare("AAA", "AA") > 0) {
			do Unit.log(l, "String2.compare len ok...", true);
		} else {
			do Unit.log(l, "String2.compare len fail...", false);
		}
		return Memory.checkEmpty();
	}
}

